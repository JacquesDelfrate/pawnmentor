# Prompts

Prompts live here as versioned files, never inlined in graph nodes. Naming convention:
`<node_name>_v<n>.md`, e.g. `classify_error_v1.md`. Bump the version suffix on any change
to prompt text — nodes reference a specific version explicitly, never "latest".
